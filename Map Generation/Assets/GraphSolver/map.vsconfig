import epackage "models/map.ecore"
import viatra "queries/queries/map.vql"

generate {
	metamodel = {package map}
	partial-model = { "instance.xmi"}
	constraints = { package queries }
	

	solver = ViatraSolver
	
	scope = {
		#<Island> += 2,
		#<Lake> += 5,
		#<City> += 3
		//#<Sea> += 1
		//#node = 5
	}
	
	number = 1
	runs = 1
	config = {
		log-level = normal
	}
	
	debug = "debug"
	output = "output/"
}